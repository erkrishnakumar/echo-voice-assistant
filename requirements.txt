# core
requests>=2.31
python-dotenv>=1.0

# database
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13

# api
fastapi>=0.110
uvicorn[standard]>=0.29

# voice
numpy>=1.24
sounddevice>=0.4          # microphone capture + playback
openwakeword>=0.6         # offline wake-word detection
# whisper.cpp and piper are BINARIES you download in setup, not pip packages

# orb animation + gesture wake trigger (optional — assistant degrades
# gracefully to wake-word-only if these aren't installed)
pygame>=2.5               # orb window
opencv-python>=4.9        # webcam capture
mediapipe>=0.10           # hand-gesture detection

# message channels (optional — a channel that can't load is skipped, not fatal)
winotify>=1.1; sys_platform == "win32"   # desktop toast notifications

# testing
pytest>=8.0